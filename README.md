Chebyshev approximants, written in pure numpy. I will try to add these to the `scipy` package, if they are wanted.

The `ChebyshevApproximant` is mostly ported from `peri`, written by @briandleahy and @mattbierbaum
